name = "blackboxprogramming-bot"
main = "src/index.js"
compatibility_date = "2024-12-01"
account_id = "848cf0b18d51e0170e0d1537aec3505a"

[vars]
ENVIRONMENT = "production"
BOT_NAME = "blackboxprogramming-bot"

# KV for session storage
[[kv_namespaces]]
binding = "MENTIONS_KV"
id = "blackboxprogramming_mentions"
preview_id = "blackboxprogramming_mentions_preview"

# Secrets (set via: wrangler secret put GITHUB_TOKEN)
# GITHUB_TOKEN — for posting comments back
# GITHUB_WEBHOOK_SECRET — for verifying GitHub webhook payloads
# BLACKROAD_GATEWAY_URL — gateway URL
# BLACKROAD_GATEWAY_TOKEN — gateway auth token
